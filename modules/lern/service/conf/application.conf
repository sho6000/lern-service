# This is the main configuration file for the Lern Service application.
# https://www.playframework.com/documentation/latest/ConfigFile
# ~~~~~

## Pekko
# https://www.playframework.com/documentation/latest/JavaPekko#Configuration
# ~~~~~
pekko {
  loggers = ["org.apache.pekko.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"
  stdout-loglevel = "INFO"
  logging-filter = "org.apache.pekko.event.slf4j.Slf4jLoggingFilter"
  log-config-on-start = off

  actor {
    provider = "org.apache.pekko.actor.LocalActorRefProvider"
    serializers {
      java = "org.apache.pekko.serialization.JavaSerializer"
    }
    serialization-bindings {
      "org.sunbird.request.Request" = java
      "org.sunbird.response.Response" = java
    }
    default-dispatcher {
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
        task-peeking-mode = "FIFO"
      }
    }
    
    # Custom Dispatchers (Merged from all services)
    rr-usr-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
      }
      throughput = 1
    }
    brr-usr-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 1
        parallelism-factor = 2.0
        parallelism-max = 4
      }
      throughput = 1
    }
    most-used-one-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
      }
      throughput = 1
    }
    most-used-two-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
      }
      throughput = 1
    }
    health-check-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 1
        parallelism-factor = 2.0
        parallelism-max = 2
      }
      throughput = 1
    }
    notification-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
      }
      throughput = 1
    }
    page-mgr-actor-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
      }
      throughput = 1
    }
    tracking-dispatcher {
      type = "Dispatcher"
      executor = "fork-join-executor"
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
      }
      throughput = 1
    }
    rr-dispatcher {
        type = "Dispatcher"
        executor = "fork-join-executor"
        fork-join-executor {
            parallelism-min = 8
            parallelism-factor = 32.0
            parallelism-max = 64
        }
        throughput = 1
    }
    brr-dispatcher {
        type = "Dispatcher"
        executor = "fork-join-executor"
        fork-join-executor {
            parallelism-min = 1
            parallelism-factor = 2.0
            parallelism-max = 4
        }
        throughput = 1
    }
    # Viewer actors get their OWN pool: the content-consumption adapter blocks on an in-JVM ask into the
    # viewer actors (monolith) — a dedicated dispatcher avoids starving the shared tracking-dispatcher.
    viewer-dispatcher {
        type = "Dispatcher"
        executor = "fork-join-executor"
        fork-join-executor {
            parallelism-min = 8
            parallelism-factor = 32.0
            parallelism-max = 64
        }
        throughput = 1
    }

    deployment {
      # --- USERORG ACTORS ---
      "/user_deletion_background_job_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = brr-usr-dispatcher }
      "/user_deletion_background_job_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_ownership_transfer_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = brr-usr-dispatcher }
      "/user_ownership_transfer_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/background_job_manager_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = brr-usr-dispatcher }
      "/background_job_manager_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_role_background_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = brr-usr-dispatcher }
      "/user_role_background_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/org_background_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = brr-usr-dispatcher }
      "/org_background_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/es_sync_background_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = brr-usr-dispatcher }
      "/es_sync_background_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/email_service_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = notification-dispatcher }
      "/email_service_actor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/user_profile_read_actor" { router = smallest-mailbox-pool, nr-of-instances = 20, dispatcher = most-used-one-dispatcher }
      "/user_profile_read_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/check_user_exist_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-one-dispatcher }
      "/check_user_exist_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/user_type_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_type_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_status_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_status_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_role_actor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = most-used-two-dispatcher }
      "/user_role_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/fetch_user_role_actor" { router = smallest-mailbox-pool, nr-of-instances = 20, dispatcher = most-used-two-dispatcher }
      "/fetch_user_role_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/user_external_identity_management_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = rr-usr-dispatcher }
      "/user_external_identity_management_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/user_self_declaration_management_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = rr-usr-dispatcher }
      "/user_self_declaration_management_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/user_org_management_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = rr-usr-dispatcher }
      "/user_org_management_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/user_on_boarding_notification_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_on_boarding_notification_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_background_job_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = most-used-two-dispatcher }
      "/user_background_job_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/user_profile_update_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-two-dispatcher }
      "/user_profile_update_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/user_login_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_login_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/org_management_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = rr-usr-dispatcher }
      "/org_management_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/search_handler_actor" { router = smallest-mailbox-pool, nr-of-instances = 25, dispatcher = most-used-one-dispatcher }
      "/search_handler_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/bulk_upload_management_actor" { router = smallest-mailbox-pool, nr-of-instances = 1, dispatcher = brr-usr-dispatcher }
      "/bulk_upload_management_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/es_sync_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = rr-usr-dispatcher }
      "/es_sync_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/file_upload_service_actor" { router = smallest-mailbox-pool, nr-of-instances = 1, dispatcher = brr-usr-dispatcher }
      "/file_upload_service_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/notes_management_actor" { router = smallest-mailbox-pool, nr-of-instances = 1, dispatcher = brr-usr-dispatcher }
      "/notes_management_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/tenant_preference_actor" { router = smallest-mailbox-pool, nr-of-instances = 1, dispatcher = rr-usr-dispatcher }
      "/tenant_preference_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/health_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = health-check-dispatcher }
      "/health_actor/*" { dispatcher = pekko.actor.health-check-dispatcher }
      "/location_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = rr-usr-dispatcher }
      "/location_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/location_background_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = brr-usr-dispatcher }
      "/location_background_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/location_bulk_upload_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/location_bulk_upload_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/org_bulk_upload_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/org_bulk_upload_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_bulk_upload_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_bulk_upload_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/system_settings_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-two-dispatcher }
      "/system_settings_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/user_tnc_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-two-dispatcher }
      "/user_tnc_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/location_bulk_upload_background_job_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/location_bulk_upload_background_job_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/org_bulk_upload_background_job_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/org_bulk_upload_background_job_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_bulk_upload_background_job_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_bulk_upload_background_job_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/otp_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = notification-dispatcher }
      "/otp_actor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/send_otp_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = notification-dispatcher }
      "/send_otp_actor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/tenant_migration_actor" { router = smallest-mailbox-pool, nr-of-instances = 3, dispatcher = brr-usr-dispatcher }
      "/tenant_migration_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/identifier_free_up_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-two-dispatcher }
      "/identifier_free_up_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/reset_password_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-one-dispatcher }
      "/reset_password_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/user_merge_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_merge_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_feed_actor" { router = smallest-mailbox-pool, nr-of-instances = 20, dispatcher = most-used-two-dispatcher }
      "/user_feed_actor/*" { dispatcher = pekko.actor.most-used-two-dispatcher }
      "/search_telemetry_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/search_telemetry_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/user_telemetry_actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-usr-dispatcher }
      "/user_telemetry_actor/*" { dispatcher = pekko.actor.brr-usr-dispatcher }
      "/send_notification_actor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/send_notification_actor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/background_notification_actor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/background_notification_actor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/user_consent_actor" { router = smallest-mailbox-pool, nr-of-instances = 3, dispatcher = rr-usr-dispatcher }
      "/user_consent_actor/*" { dispatcher = pekko.actor.rr-usr-dispatcher }
      "/user_lookup_actor" { router = smallest-mailbox-pool, nr-of-instances = 25, dispatcher = most-used-one-dispatcher }
      "/user_lookup_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/user_update_actor" { router = smallest-mailbox-pool, nr-of-instances = 20, dispatcher = most-used-one-dispatcher }
      "/user_update_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/managed_user_actor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = most-used-one-dispatcher }
      "/managed_user_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/ssu_user_create_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-one-dispatcher }
      "/ssu_user_create_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }
      "/sso_user_create_actor" { router = smallest-mailbox-pool, nr-of-instances = 15, dispatcher = most-used-one-dispatcher }
      "/sso_user_create_actor/*" { dispatcher = pekko.actor.most-used-one-dispatcher }

      # --- LMS ACTORS ---
      "/page-management-actor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = page-mgr-actor-dispatcher }
      "/cache-management-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/course-metrics-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/course-enrolment-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = tracking-dispatcher }
      "/content-consumption-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = tracking-dispatcher }
      "/course-batch-management-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/search-handler-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/health-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/bulk-upload-management-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/es-sync-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/bulk-upload-background-job-actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-dispatcher }
      "/background-job-manager-actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-dispatcher }
      "/course-batch-certificate-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/certificate-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/qrcode-download-management-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/course-batch-notification-actor" { router = smallest-mailbox-pool, nr-of-instances = 2, dispatcher = brr-dispatcher }
      "/course-management-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/group-aggregates-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/collection-summary-aggregate-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/exhaust-job-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/assessment-aggregator-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }
      "/activity-aggregator-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }

      # --- OBSERVABILITY ACTORS ---
      "/observability-report-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }

      # --- VIEWER ACTORS --- (dedicated viewer-dispatcher; see dispatcher comment above)
      # viewer-aggregator-actor is NOT here: it is bound programmatically in LernServiceActorStartModule
      # with a ConsistentHashingPool + hashMapper keyed on userId (config FromConfig cannot supply a
      # hash key, and unhashable messages would go to deadLetters).
      "/view-consumption-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = viewer-dispatcher }
      "/viewer-summary-actor" { router = smallest-mailbox-pool, nr-of-instances = 4, dispatcher = rr-dispatcher }

      # --- NOTIFICATION ACTORS ---
      "/HealthActor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = notification-dispatcher }
      "/HealthActor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/NotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/NotificationActor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/CreateNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/CreateNotificationActor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/ReadNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/ReadNotificationActor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/UpdateNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/UpdateNotificationActor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/DeleteNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
      "/DeleteNotificationActor/*" { dispatcher = pekko.actor.notification-dispatcher }
      "/NotificationTemplateActor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = notification-dispatcher }
      "/NotificationTemplateActor/*" { dispatcher = pekko.actor.notification-dispatcher }

    }
  }

  notification-dispatcher {
    type = "Dispatcher"
    executor = "fork-join-executor"
    fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
    }
      throughput = 1
  }
}

notificationActorSystem {
  default-dispatcher {
    type = "Dispatcher"
    executor = "fork-join-executor"
    fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
    }
    throughput = 1
  }
  notification-dispatcher {
    type = "Dispatcher"
    executor = "fork-join-executor"
    fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 32.0
        parallelism-max = 64
    }
    throughput = 1
  }
  pekko {
    actor {
      notification-dispatcher {
        type = "Dispatcher"
        executor = "fork-join-executor"
        fork-join-executor {
            parallelism-min = 8
            parallelism-factor = 32.0
            parallelism-max = 64
        }
        throughput = 1
      }
      deployment {
          # --- NOTIFICATION ACTORS ---
          "/HealthActor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = default-dispatcher }
          "/NotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
          "/CreateNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
          "/ReadNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
          "/UpdateNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
          "/DeleteNotificationActor" { router = smallest-mailbox-pool, nr-of-instances = 10, dispatcher = notification-dispatcher }
          "/NotificationTemplateActor" { router = smallest-mailbox-pool, nr-of-instances = 5, dispatcher = notification-dispatcher }
      }
    }
  }
}

## Internationalisation
play.i18n {
  langs = [ "en" ]
}

## Play HTTP settings
play.http {
  errorHandler = modules.ErrorHandler
  actionCreator = modules.OnRequestHandler
  parser.maxDiskBuffer=50MB
  parser.maxMemoryBuffer=50MB
  
  # HOCON: last assignment wins, so the dev fallback is set first and the env var override comes last.
  # If APPLICATION_SECRET is set, it takes priority. If absent (${?...} is a no-op), the dev value is kept.
  secret.key = "lernservice:RBl6NfkoO5tNYblTRmf3ZLcDIp5@oVjJMDFHdR74?tDvH2n" # Fallback for dev only
  secret.key = ${?APPLICATION_SECRET}   # Production override — takes priority when env var is set
}

## Netty Provider
play.server {
  provider = "play.core.server.NettyServerProvider"
  netty {
    eventLoopThreads = 30
    transport = "native"
    maxChunkSize = 30000000
    option {
      child.SO_KEEPALIVE = true
    }
    http {
      idleTimeout = infinite
    }
  }
}

## WS (HTTP Client)
libraryDependencies += javaWs

## Cache
libraryDependencies += cache
play.cache {
  # bindCaches = ["db-cache", "user-cache", "session-cache"]
}

# Logger
logger.root=ERROR
logger.play=OFF
#logger.application=DEBUG

# Authentication
AuthenticationEnabled = true
AUTH_ENABLED = true

# Service Specific Configs

# Assessment Aggregator Configuration (LMS)
assessment_direct_aggregation_enabled=true
kafka_assessment_topic="sunbird.assessment.raw"
kafka_topics_contentstate_invalid="sunbird.contentstate.invalid"
assessment_skip_missing_records=true

# Notification specific
sunbird_notification_keyspace="sunbird_notifications"

# APP Specific config
play.modules {
  enabled += modules.LernServiceStartModule
  enabled += modules.LernServiceActorStartModule
  enabled += modules.StorageModule

  # Disable service-specific start modules as they are replaced by MonolithicStartModule/MonolithicActorStartModule
  disabled += modules.StartModule
  disabled += modules.ActorStartModule
  disabled += utils.module.StartModule
}

## Observability Transform Config
# Maps utility keys to the request field names that trigger them,
# and defines the fields to return and cache settings for each utility.
observability {
  transform {
    field-mapping {
      user       = ["userid", "user_id", "userIdentifier", "createdBy"]
      collection = ["courseid", "course_id", "object_id", "courseIdentifier"]
      content    = ["content_id", "contentId", "contentIdentifier"]
    }
    user {
      fields         = ["firstName", "lastName", "maskedEmail"]
      result-key     = "userDetails"
      cache-ttl      = 86400     # seconds; user profiles change infrequently
      cache-max-size = 1000    # max LRU entries (~200KB heap)
    }
    collection {
      fields         = ["name", "identifier", "contentType"]
      result-key     = "collectionDetails"
      cache-ttl      = 86400    # seconds; collections change very rarely
      cache-max-size = 500     # max LRU entries
    }
    content {
      fields         = ["name", "identifier", "contentType"]
      result-key     = "contentDetails"
      cache-ttl      = 86400    # seconds; content metadata changes very rarely
      cache-max-size = 1000    # max LRU entries
    }
  }
}

# Redis optional support — master switch
# When false (default), Redis is not required. All Redis use cases are either skipped or fall back to in-memory alternatives.
redis.enabled = false

play.filters {
  hosts {
    allowed = ["localhost:9000","."]
  }
  enabled += filters.AccessLogFilter
  enabled += filters.CustomGzipFilter
  # Disabled ResponseFilter to prevent excessive response body logging
  # which was causing performance issues and Keycloak retry loops
  # enabled += filters.ResponseFilter
  disabled += filters.ResponseFilter
  disabled += play.filters.csrf.CSRFFilter
}
